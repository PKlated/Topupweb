@{
    ViewData["Title"] = "Home";
    Layout = "~/Views/Shared/_Layout.navbar.cshtml";
}

<!-- HERO -->
<div class="bg-white border-4 border-black shadow-[6px_6px_0_#000] p-6 mb-8">
    <h1 class="text-3xl font-extrabold tracking-wide mb-2">
        Welcome to GAMEWEB
    </h1>
    <p class="text-sm text-gray-700">
        Top up, events, promotions, and in-game rewards all in one place.
    </p>

    <div class="mt-4 flex space-x-3">
        <!-- FIXED: User/TopUp -->
        <a asp-controller="User" asp-action="TopUp"
           class="px-4 py-2 border-2 border-black bg-[#FFCB05]
                  shadow-[3px_3px_0_#000] hover:translate-x-[1px]
                  hover:translate-y-[1px] hover:shadow-none font-bold">
            TOP-UP NOW
        </a>

        <a asp-controller="Event" asp-action="Index"
           class="px-4 py-2 border-2 border-black bg-white
                  shadow-[2px_2px_0_#000] hover:translate-x-[1px]
                  hover:translate-y-[1px] hover:shadow-none font-bold">
            VIEW EVENTS
        </a>
    </div>
</div>

<!-- INFO GRID -->
<div class="grid md:grid-cols-3 gap-6">
    <div class="bg-white border-4 border-black p-4 shadow-[4px_4px_0_#000]">
        <h3 class="font-extrabold mb-2">🔥 Promotion</h3>
        <p class="text-sm">Limited-time bonuses and discounts.</p>
    </div>

    <div class="bg-white border-4 border-black p-4 shadow-[4px_4px_0_#000]">
        <h3 class="font-extrabold mb-2">🎉 Events</h3>
        <p class="text-sm">Join in-game events and earn rare items.</p>
    </div>

    <div class="bg-white border-4 border-black p-4 shadow-[4px_4px_0_#000]">
        <h3 class="font-extrabold mb-2">📦 Inventory</h3>
        <p class="text-sm">
            @if (User.Identity.IsAuthenticated)
            {
                <span>View your owned items and rewards.</span>
            }
            else
            {
                <span class="text-gray-500">Login required to view inventory.</span>
            }
        </p>
    </div>
</div>
